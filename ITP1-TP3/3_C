from re import split


while True:
    a,b = map(int, input(),split())
    if a == 0 and b == 0: # aとbの両方が0の場合、ループを抜ける
        break
    if a > b: # aがbよりも大きい場合はbから表示する
        print("%d %d"%(b,a))
    else:
        print("%d %d"%(a,b))